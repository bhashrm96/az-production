import { EventsCreateView } from 'src/sections/events/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Create a new event',
};

export default function UserCreatePage() {
  return <EventsCreateView />;
}
