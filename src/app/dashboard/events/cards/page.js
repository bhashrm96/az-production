import { EventsCardsView } from 'src/sections/events/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Events Cards',
};

export default function EventsCardsPage() {
  return <EventsCardsView />;
}
